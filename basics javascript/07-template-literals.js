// Implantato no ES6, usado com crase (`frase exemplo`) para delimitar a declaração de string
const example = `example string`;


// Com ela pode-se usar aspas duplas e simples dentro da string sem impedimneto (`uma 'frase' de "exemplo"`)
const display = `Wellcome to drink's Bar`; // Wellcome to drink's Bar. 

// Também pode-se interpolar Código JavaScript
const myName = 'Jorge William';
const age = 36;
const presentation = `Hello my name's ${myName}, and i will be ${age + 1} next year !`;

// Se precisar usar o caracter ` (crase) dentro de uma frase usamos o escape \` (contrabarra)

const escape = `My string with backtick inside: \` `